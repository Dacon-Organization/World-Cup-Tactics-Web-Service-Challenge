# DESIGN.md — RE:FORMATION 디자인 시스템 v1.0

> **이 문서는 창작물이 아니라 역추출물입니다.** 기획서 v2.0 PDF(제출 완료·동결)에 실린
> 그림 14종이 이미 시각 정체성을 확정했습니다. 이 문서는 그 그림들을 만든
> [`dev/typeset/make_figures.py`](dev/typeset/make_figures.py)의 실제 상수를 읽어
> 구현용 토큰으로 옮긴 것입니다.
>
> **왜 역추출인가** — 심사 항목 "기획 의도와 실제 구현의 일관성"(20점)은 기획서에 실린
> 그림과 배포된 화면이 같아 보일 때 충족됩니다. 새로 예쁘게 만드는 것보다 **제출한 그림과
> 어긋나지 않는 것**이 점수입니다. 따라서 색을 고르는 자유는 이미 없고, 남은 일은
> 그림에 없는 것(상태·간격·모션·접근성)을 그림과 모순 없이 채우는 것입니다.
>
> 근거 표기: `(Pn)` 리서치 원장 · `ADR-n` 설계 결정 기록 · `[설계 결정]` 이 문서의 국소 재량
> · `[추정 설계목표]` 구현 후 실측 검증

---

## 0. 원본 대조표 — 이 문서가 무엇을 읽었는가

| 토큰 출처 | 파일·위치 | 확인한 것 |
|---|---|---|
| 다크 팔레트 | `make_figures.py:739~778` (`f13_share_card`) | 배경·텍스트 4계단·슬라이더 트랙·노브 |
| 결과 3색 | `make_figures.py:42~44` (`GREEN`/`AMBER`/`RED`) | 승·무·패 |
| 포지션 색 | `make_figures.py:478` (`POS_COLORS`) | GK·DF·MF·FW |
| 포메이션 좌표 | `make_figures.py:472~477` (`FORMATION_433`) | 4-3-3 11슬롯 정규화 좌표 |
| 피치 기하 | `make_figures.py:494~505` (`draw_pitch`) | 센터서클·페널티박스·골박스 비율 |
| 레이아웃 | `figures/f10_wireframe_board.png` | 모바일·데스크톱 골격 |
| 타이포 | `make_figures.py:51`, `dev/typeset/preamble.tex` | Pretendard |

**이 표의 용도**: 구현 중 "이 색 맞나?"가 생기면 이 문서가 아니라 위 원본을 봅니다.
이 문서와 원본이 어긋나면 **원본이 우선**이고 이 문서를 고칩니다.

---

## 1. 컬러 토큰

### 1.1 서피스·텍스트 (다크 단일)

```css
--bg:            #10161F;  /* 페이지 배경 — f13 카드 배경과 동일 */
--surface:       #1A2028;  /* 패널·카드 [설계 결정: bg에서 한 단 밝게] */
--surface-raised:#232B35;  /* 시트·모달 [설계 결정] */
--track:         #2A3442;  /* 슬라이더 빈 트랙 — f13 실측 */
--border:        #2A3442;  /* 구분선 — track과 동일값 재사용 */

--text:          #FFFFFF;  /* 본문·수치 */
--text-2:        #8E9CAD;  /* 라벨·보조 설명 */
--text-3:        #6B7A8C;  /* 메타(반복 횟수·신뢰구간) */
--text-4:        #55636F;  /* 고지문(가공명 표기·비공식 팬 프로젝트) */
```

**텍스트 4계단이 있는 이유** — 이 서비스의 화면은 수치가 촘촘합니다(확률·신뢰구간·반복
횟수·출처 라벨·고지문). 크기만으로 위계를 만들면 모바일에서 전부 작아져 읽히지 않으므로,
**밝기로 위계를 만들고 크기는 가독 하한을 지킵니다.** f13 카드가 이미 이 방식입니다.

### 1.2 브랜드

```css
--brand:      #1F4E79;  /* ACCENT — 컨트롤 채움·상단 바 */
--brand-lit:  #7FB3E0;  /* 워드마크·슬라이더 노브·포커스 링 */
```

### 1.3 결과 3색 (승·무·패) — 의미 고정

```css
--win:  #2E7D5B;
--draw: #C98A21;
--lose: #B0413E;
```

이 3색은 **의미에 고정**됩니다. 다른 용도로 재사용하지 않습니다.

### 1.4 포지션 4색 — 결과 3색과 값이 같다 (충돌 고지)

```css
--pos-gk: #C98A21;  /* = --draw */
--pos-df: #1F4E79;  /* = --brand */
--pos-mf: #2E7D5B;  /* = --win  */
--pos-fw: #B0413E;  /* = --lose */
```

**이것은 오류가 아니라 제출된 상태입니다.** `POS_COLORS = [AMBER] + [ACCENT]*4 +
[GREEN]*3 + [RED]*3` 이 f13 공유 카드와 f10 와이어프레임에 그대로 실렸습니다.

색을 바꾸면 제출한 PDF와 배포 화면이 달라 보이므로 **색은 그대로 두고 형태로 분리**합니다
`[설계 결정]`:

| 구분 규칙 | 내용 |
|---|---|
| **형태 분리** | 선수 토큰 = **원(circle)** · 확률 10×10 배열 = **모난 사각(rounded-square, r=2px)** · 확률 바 = **가로 막대** |
| **영역 분리** | 선수 토큰은 피치 안에만, 결과색은 예측 패널 안에만 — 한 컨테이너에 두 의미의 같은 색이 공존하지 않음 |
| **라벨 강제** | 결과색이 쓰인 곳에는 항상 텍스트가 붙는다 ("승 62" — f13이 이미 그렇다). 색만으로 의미를 전달하는 표면 0개 |

**왜 형태 분리가 충분한가** — S1 한 화면에 피치의 원형 토큰 11개와 예측 패널의 10×10
배열 100개가 동시에 존재하는 것이 유일한 실질 충돌 지점입니다. 원 vs 사각은 색맹 여부와
무관하게 구별되고, 44px 토큰과 8px 배열 점은 크기 차이도 5배 이상입니다.

> **미해결로 남기는 것**: 포지션은 색상 링 단독으로 부호화됩니다(토큰 내부 텍스트는 포지션이
> 아니라 가공명 성 1음절 — [가공명_체계 §5.1](docs/planning/data/version1.0/가공명_체계_v1_0.md)).
> 즉 **포지션은 색맹 사용자에게 전달되지 않습니다.** 보완: 토큰 포커스·탭 시 툴팁에
> 포지션 문자를 명시하고, 슬롯 자체가 피치 상 y좌표로 역할을 암시하게 둡니다.
> 링에 문자 글리프를 넣는 안은 44px 안에서 성 1음절과 경합해 가독을 해치므로 기각합니다.

### 1.5 상태색

```css
--focus:   #7FB3E0;  /* 포커스 링 — brand-lit 재사용 */
--warn:    #C98A21;  /* "간이 추정" 배지·자동 강등 표기 — draw와 동일 계열 [설계 결정] */
--disabled:#55636F;
```

`--warn`이 `--draw`와 같은 값인 것은 의도입니다 — 둘 다 "확정이 아님"을 뜻하고,
배지는 항상 텍스트를 동반하므로 혼동 표면이 없습니다.

---

## 2. 타이포그래피

**서체: Pretendard (SIL OFL 1.1)** — 조판 파이프라인(`preamble.tex`)과 동일 서체를 쓰는
것이 핵심입니다. 기획서 PDF와 배포 화면의 글자 모양이 같아야 일관성이 성립합니다.

- 서브셋 `woff2` **자체 호스팅** (`public/fonts/`). CDN 금지 — 외부 요청 0건 원칙(C1)과
  "심사자 키 없이 확인"(요강) 정합. 서브셋 = KS X 1001 한글 + Latin + 숫자·기호
- `font-display: swap`, 폴백 `-apple-system, "Malgun Gothic", sans-serif`
- **한자 tofu 주의**: Pretendard는 한자 글리프가 없습니다 — 조판 파이프라인이 한자 병기를
  제거하는 이유이며([dev/typeset/README.md](dev/typeset/README.md)), 화면에서도 동일합니다.
  UI 문자열에 한자를 쓰지 않습니다 — "가공명 성(姓)" 같은 병기 표기도 UI에서는 제외

| 역할 | 크기 / 굵기 / 자간 | 쓰이는 곳 |
|---|---|---|
| 확률 수치 | 40px / 700 / -0.02em | 승리 확률 대표 숫자 |
| 확률 바 라벨 | 18px / 700 | "승 62" |
| 화면 제목 | 22px / 700 | "같은 체코전, 다른 전술" |
| 섹션 라벨 | 15px / 600 | "전술 슬라이더" |
| 본문 | 15px / 400 | 설명문 |
| 슬라이더 라벨 | 14px / 400 | "라인 높이" |
| 메타 | 13px / 400 | "5,000회 시뮬레이션 · 95% 구간 ±1.4%p" |
| 고지 | 12px / 400 | "가공명 표기 · 비공식 팬 프로젝트" |
| 토큰 성 1음절 | 15px / 700 | 선수 토큰 |

**하한 규칙**: 모바일에서 **12px 미만을 쓰지 않습니다.** f13 카드의 8.5pt 고지문은 인쇄물
기준이며, 화면에서는 12px로 올립니다 `[설계 결정]`.

---

## 3. 간격·레이아웃

4px 기반 스케일: `2 / 4 / 8 / 12 / 16 / 24 / 32 / 48`.

### 3.1 브레이크포인트 — Mobile First

| 이름 | 폭 | 레이아웃 |
|---|---|---|
| base | ~767px | 1열 세로 — 헤더 / 피치 / 예측 패널 / 슬라이더 시트 |
| md | 768~1023px | 1열 유지 + 슬라이더 상시 노출(시트 해제) |
| lg | 1024px~ | 2열 — 피치 2/3 · 우측 패널 1/3(슬라이더 상단·예측 하단) |

`f10_wireframe_board.png`의 두 그림이 base와 lg입니다. md는 그림에 없어 신설했습니다
`[설계 결정]` — 태블릿에서 시트 스와이프는 화면이 남는데도 조작을 숨기는 손해입니다.

### 3.2 S1 세로 예산 (base, 375×667 iPhone SE 기준)

| 영역 | 높이 | 근거 |
|---|---|---|
| 헤더 | 52px | 모드 탭 터치 타겟 |
| 피치 | `min(56vh, 가로 × 1.5)` | 세로형 피치 종횡비 유지 |
| 예측 패널 | 132px (고정) | 확률 바 + 메타 1줄 + 배지 여유 |
| 슬라이더 시트 핸들 | 40px | 나머지 |

**예측 패널을 고정 높이로 두는 이유** — 확률이 갱신될 때 패널 높이가 변하면 피치가
위아래로 흔들려 드래그 중인 손가락과 좌표가 어긋납니다. 값이 바뀌어도 레이아웃은
움직이지 않습니다(CLS 0).

### 3.3 피치 기하 (`draw_pitch` 비율 이식)

세로형. 폭 `w`, 높이 `h` 기준 — SVG `viewBox`로 그대로 옮깁니다.

| 요소 | 비율 |
|---|---|
| 센터 서클 반지름 | `min(w,h) × 0.11` |
| 페널티 박스 | x: `w×0.22` 부터 `w×0.56` · 높이 `h×0.13` |
| 골 박스 | x: `w×0.36` 부터 `w×0.28` · 높이 `h×0.055` |
| 라인 | `#FFFFFF` / opacity 0.55 / 1.1px |

**전부 자체 제작 도형** — 외부 피치 이미지·에셋 0개 (라이선스 방어).

### 3.4 슬롯 좌표 원본 (4-3-3)

`formations.json`의 `f433`은 아래 값을 그대로 씁니다. y=0이 자기 진영, y=1이 상대 진영
(데이터 설계 §2.2 `PositionSlot` 정의와 일치).

```
GK  (0.50, 0.06)
DF  (0.16, 0.24) (0.38, 0.20) (0.62, 0.20) (0.84, 0.24)
MF  (0.28, 0.48) (0.50, 0.44) (0.72, 0.48)
FW  (0.20, 0.74) (0.50, 0.80) (0.80, 0.74)
```

나머지 3프리셋(f442·f343·f352)은 이 좌표계 규칙(대칭·라인 간격 0.20~0.26)을 따라
B2에서 신설합니다 `[설계 결정]`.

---

## 4. 컴포넌트 인벤토리

| 컴포넌트 | 담당 기능 | 핵심 규격 |
|---|---|---|
| `Pitch` | F01 | 자체 SVG, §3.3 비율, 슬롯 히트영역 포함 |
| `PlayerToken` | F01·F02·F03 | **원형 44px 이상**, 포지션 색 링 3px, 성 1음절 |
| `SliderSheet` | F04 | 4종. 트랙 `--track`, 채움 `--brand`, 노브 `--brand-lit` 14px |
| `ProbabilityBar` | F07 | 가로 3분할 막대 30px, 세그먼트 내부 텍스트 강제 |
| `DotGrid10x10` | F07 | **모난 사각** 8px, 간격 2px, 색은 결과 3색 |
| `WilsonBand` | F07 | 막대 아래 구간 표시 + 수치 병기 |
| `HopsPlayer` | F07 | 프레임 애니메이션. `prefers-reduced-motion` 시 정지 프레임 |
| `EstimateBadge` | F05 | "간이 추정" — `--warn` 테두리 + 텍스트 |
| `MatchCard` | F09 | 국기(flag-icons MIT) + 라운드 + 개최지 + 스코어 |
| `TimelineScrubber` | F10 | 분 단위 이벤트 마커, 시점 선택 = 개입점 |
| `WinProbChart` | F12 | 538식 2라인 (실제 실선 · 시나리오 파선) |
| `ShareCard` | F14·F16 | f13 레이아웃 그대로 1200×630 |

**`WinProbChart`의 선 스타일이 색이 아닌 이유** — 실제/시나리오 구분은 색맹 사용자에게도
전달되어야 하므로 실선·파선으로 부호화합니다 (F12-R3).

---

## 5. 모션

지연 예산표([기능명세 인덱스 §공통 지연 예산표](docs/planning/features/version1.0/기능명세_인덱스_v1_0.md))와 1:1로 맞춥니다.

| 대상 | 시간 / 이징 | 예산 근거 |
|---|---|---|
| 슬라이더 → 시각 프리뷰 | 즉시 (transition 없음) | ≤100ms (P5) — transition을 넣으면 예산을 씀 |
| 토큰 슬롯 스냅 | 120ms `cubic-bezier(.2,.8,.2,1)` | ~100ms D&D 피드백 |
| 확률 바 세그먼트 폭 | 220ms `ease-out` | 지연 예산 없음. 변화를 인지시키는 용도 |
| 시트 열기·닫기 | 260ms `ease-out` | — |
| HOPs 프레임 | 400ms/프레임 | 불확실성 지각 유지 |

- **`prefers-reduced-motion: reduce`**: 확률 바 폭 전환·시트 애니메이션·HOPs 자동 재생을
  모두 끄고, HOPs는 정지 프레임 + 수동 넘김으로 강등합니다.
- **스켈레톤 금지 구간**: S1 첫 페인트는 스켈레톤이 아니라 `defaults.json.precomputed`의
  **실제 확률**입니다 (F08 — 빈 화면 금지). 스켈레톤은 리플레이 차트에만 씁니다.

---

## 6. 접근성

| 항목 | 기준 | 적용 |
|---|---|---|
| 터치 타겟 | ≥44px (P5 "1cm") | 선수 토큰·슬라이더 노브·시점 마커 |
| 드래그 대안 | **WCAG 2.5.7 AA** | 클릭-클릭 + 키보드 (F03) — D&D와 결과 동일 보장 |
| 포커스 표시 | 2px `--focus` 외곽 + 2px 오프셋 | 전 인터랙티브 요소 |
| 색 단독 의존 | 금지 | 결과=텍스트 병기 · 차트=선 스타일 · 포지션=툴팁 보완(§1.4 한계 고지) |
| 명도 대비 | 본문 4.5:1 / 큰 글자 3:1 | `--text-4`(#55636F on #10161F ≈ 3.9:1)는 **12px 이상 + 장식성 고지문에만** `[추정 설계목표: 실측]` |
| 스크린리더 | 확률 갱신 `aria-live="polite"` | 조작마다 읽히지 않게 debounce 후 1회 |
| 축소·확대 | 400% 확대에서 가로 스크롤 없음 | `[추정 설계목표: B8 실측]` |

**`--text-4` 대비가 미달인 것을 숨기지 않습니다** — 해당 색은 "가공명 표기 · 비공식 팬
프로젝트" 고지문 전용이고, 이 문구는 법적 고지 목적이라 **`--text-3`로 올리는 것을
B8 실측 후 결정**합니다. 대비 미달 상태로 확정하지 않습니다.

---

## 7. 이 시스템이 쓰지 않는 것 (라이선스·실격 방어)

| 금지 | 이유 |
|---|---|
| 선수 사진·초상 | 퍼블리시티권·저작권 (P7) |
| 팀 엠블럼·유니폼·FIFA 로고·대회 워드마크 | 상표권 (P7·기획서 13절) |
| 실명·이니셜·연상형 변형명 | 서울서부지법 2010카합245 (P7) |
| 외부 피치·아이콘 이미지 | 자체 SVG로 대체 — 출처 관리 비용 제거 |
| 폰트 CDN·외부 스타일시트 | 외부 요청 0건 원칙(C1) |
| 라이트 모드 | 7일 내 QA 비용 대비 이득 없음. 다크 단일 `[설계 결정]` |

**허용**: 국가명 표기(P7) · 국기(flag-icons MIT) · Pretendard(SIL OFL 1.1) · shadcn/ui(MIT).

---

## 8. 검증 (B8 QA 이관 항목)

- [ ] `--text-4` 명도 대비 실측 → 미달 시 `--text-3`로 승격
- [ ] 375×667에서 §3.2 세로 예산이 실제로 맞는지 (피치 잘림 0)
- [ ] 확률 갱신 시 CLS 0 (예측 패널 고정 높이 검증)
- [ ] 400% 확대 가로 스크롤 0
- [ ] `prefers-reduced-motion` 강제 시 자동 애니메이션 0건
- [ ] 배포 화면 스크린샷 vs `f10`·`f13` 그림 나란히 대조 — 색·배치 어긋남 0
- [ ] 색맹 시뮬레이션(3종)에서 승/무/패 구별 가능 (텍스트 병기 확인)
