# 분석 노트북 개정안 — 2026-07-23 사용자 피드백 반영 설계

> **이 문서는 다음 세션의 착수 문서입니다.** 노트북_서사 v1.0의 버전업 재료이며(작업 중 버전업
> 금지 원칙에 따라 v1.1 일괄 반영 전까지 이 개정안이 작업 기준), 근거 리서치는
> [P18](../../research/findings/P18-분석도구생태계.md).

---

## 1. 발단 — 사용자 피드백 3건 (2026-07-23, 원문 요지)

| # | 피드백 | 진단 |
|---|---|---|
| ① | 데이터 수집은 끝냈지만 **어떻게 했는지의 서사가 없다**. 탐색을 정말 상세하게 해야 하며 **바로 EDA로 넘어가는 것은 좋지 않다** | 현행 01장 1-1은 코드 몇 줄. "탐색"이라는 독립 단계가 구조에 없음 |
| ② | 탐색→전처리/가공→EDA→모델링→해석 과정을 **반복하면서 버전 관리** — "처음엔 이랬는데 무슨 문제를 발견해 수정했는지" | 9장 선형 구조에 반복 사이클 관점 부재 |
| ③ | GitHub 고스타·HF 등 도구를 **가져와 커스터마이징**. 전문가들이 만든 흐름을 MCP·스킬처럼 재사용 | → P18 리서치로 회수 완료 |

**확정 결정 (사용자 답변)**: ⑴ 이 세션은 리서치+설계 기록까지, 노트북 개정은 새 세션
⑵ 00장 신설 방식 ⑶ 개정 이력 셀 + 중앙 원장(ITERATION-LOG) 방식 ⑷ P18 조사 범위 4종 전부.

## 2. 노트북 구조 개정안 — 00장 신설 (01~09 번호 불변)

| 장 | 상태 | 내용 |
|---|---|---|
| **00 수집·탐색** (`00_collect_explore.ipynb`) | **신설** | §3 참조 |
| 01 EDA | 보강 | 수집·탐색 절(1-1~1-3)을 00장으로 이관하고, 서두에 "전처리 결정" 절 신설 — 00장 탐색에서 발견한 사실 → 전처리 방침(타입 캐스팅·남녀 분리·결측 처리)의 근거 연결. pandera 게이트로 00장 산출물 검증 후 진입 |
| 02 FE | 보강 | pandera 스키마 게이트 + 개정 이력 셀 추가 (Elo 로직은 불변) |
| 03~09 | 예정 (신규 작성 시 적용) | 각 장 서두에 ① 개정 이력 셀 ② 이전 장 산출물 pandera 게이트 |

**번호 유지 이유**: 기획서 10절·노트북_서사 §2의 장 번호 참조가 전부 보존됨. 00장은 "본편
이전의 재료 검토"로 서사상도 자연스러움.

## 3. 00장(수집·탐색) 구성안

### 3-1. 수집의 서사 (피드백 ① 전반부)

1. **출처 선정의 이유** — 왜 `jfjelstul/worldcup`인가: P2(후보 감사)→P10(문구 단위 대조)의
   결정 과정을 노트북 안에서 요약 재현 (StatsBomb 재업로드 기각 사유 포함). CC BY-SA 4.0
   고지 → [DATA-LICENSE.md](../../../DATA-LICENSE.md) 링크
2. **수집 방법 상세** — raw.githubusercontent 직접 다운로드 코드 + 커밋 핀 고정(재현성) +
   파일 크기·행수·체크섬 기록 표
3. **캐시 구조** — cookiecutter-data-science 규약(P18 Q5) 채택: `data/raw/`(불변 원본),
   `data/interim/`(마스킹·정제), `data/processed/`(피처 테이블). **raw는 절대 수정 금지** —
   반복 사이클에서 어느 단계 산출물인지 경로가 말해주는 구조
4. **실명 마스킹 셀 이동** — ⚠️ 현행 01장 1-3의 마스킹 셀(NB-R1)을 **00장 로드 직후로
   이동**. 00장이 원본을 만지는 첫 지점이 되므로 경계도 함께 앞당겨야 프로파일링
   출력(값 샘플 노출)에 실명이 새지 않음 (P18 Q1 채택 조건 1과 정합)

### 3-2. 탐색의 상세 (피드백 ① 후반부 — "바로 EDA로 넘어가지 않는다")

EDA(분포·관계의 통계 분석)와 구분되는 **탐색(데이터 실체 파악)** 을 전수 수행:

| 절 | 내용 | 도구 (P18 채택) |
|---|---|---|
| 테이블 전수 조사 | 4개 CSV 각각: 행수·컬럼·dtype·메모리·키 후보 | pandas 직접 |
| 스키마·키 무결성 | PK 유일성, 테이블 간 FK 정합(match_id 등), 고아 레코드 | pandera `DataFrameSchema` |
| 결측 구조 | 컬럼별 결측률 + **결측의 패턴**(특정 연대·대회에 몰리는가) | missingno matrix·bar |
| 프로파일 요약 | 수치·범주 컬럼 통계 일괄 → **커스텀 요약 표**로 압축 표시 | fg-data-profiling `minimal=True` → json 추출 (HTML 통짜 임베드 금지) |
| 값 탐색 | 범주 유니크 값 목록, 이상값 후보(음수 득점 등), 시대별 커버리지 | pandas 직접 |
| 탐색 결론 셀 | "발견한 사실 → 01장 전처리 결정에 넘기는 목록" 마크다운 | — |

**도구는 보조, 서사는 사람** (P18 채택 조건 3): 도구 출력은 출발점 표로 쓰고 각 이상
신호의 해석은 직접 서술. 각 절은 "확인 질문 → 코드 → 답" 구조 유지.

## 4. 반복 사이클 규약 (피드백 ②)

```
탐색(00) → 전처리 결정(01) → EDA(01) → 가공(02) → 모델링(03~07) → 해석(08~09)
   ↑                                                        │
   └────────── 문제 발견 시 해당 상류 단계로 복귀 ──────────┘
```

1. **사이클 정의**: 하류 단계(모델링·해석)에서 발견한 문제로 상류 단계(탐색·전처리·가공)를
   고치는 1회 왕복 = 사이클 1회. 노트북 버전은 `c1`(최초 작성), `c2`, … 로 부른다
2. **기록 이중화**:
   - **각 노트북 서두 "개정 이력" 셀** — 표 형식: `사이클 | 날짜 | 발견(무엇이 문제였나) |
     원인 | 수정 | 영향 범위`. 최초 작성도 c1로 기록
   - **중앙 원장 [notebooks/ITERATION-LOG.md](../../../notebooks/ITERATION-LOG.md)** —
     사이클 단위 상세(발견 경위·대안 비교·기각안 포함). 노트북 셀은 요약, 원장이 정본
     (RESEARCH-LOG와 같은 철학: 발견이 사라지지 않게)
3. **기계 게이트**: 각 장 진입 시 이전 장 산출물을 pandera 스키마로 검증 — 상류 수정이
   하류 계약을 깨면 즉시 명시적 에러 (NB-R5의 도구화, 02장 리키지 assert의 일반화)
4. **노트북_서사 v1.1 반영 예정 조항** (버전업 시 기계적 적용 목록):
   - §2 표에 00장 행 추가, §3에 00장 카드 추가
   - §4 마스킹 경계를 "01장 서두" → "00장 로드 직후"로 개정
   - §5 실행 순서 "01→09" → "00→09"
   - §6에 NB-R7 신설: **WHEN** 사이클 개정이 발생하면 개정 이력 셀과 ITERATION-LOG에
     동시 기록된다 / NB-R8: **IF** 장 진입 pandera 게이트가 실패하면 **THEN** 하류 실행을
     중단하고 원인을 ITERATION-LOG에 기록한다

## 5. 도구 채택·적용 지점 (P18 결론 매핑)

| 도구 | 버전(실측) | 적용 지점 |
|---|---|---|
| fg-data-profiling (구 ydata-profiling, **2026-04 개명 — 옛 이름으로 설치 금지**) | 4.18.4 | 00장 프로파일 요약 (minimal=True, 마스킹 후, json 추출) |
| pandera | 0.32.1 | 전 장 진입 게이트 — 스키마는 `src/analysis/schemas.py`로 모듈 분리 (노트북_서사 §1 허용 범위) |
| missingno | 0.5.x | 00장 결측 구조 시각화 |
| cookiecutter-data-science 규약 | — | `data/raw·interim·processed` 계층 + 원본 불변 원칙 |

requirements.txt에 3종 버전 고정 추가 (노트북_서사 §5 재현 규약).

## 6. 미결 사항 (새 세션 착수 전 결정 필요)

| # | 항목 | 내용 | 권고 |
|---|---|---|---|
| 1 | **martj42/international_results 채택 여부** | CC0 남자 A매치 49,459+. Elo 입력을 월드컵 한정→전체 A매치로 확장하면 FT-R7 공백 감쇠의 근본 원인 완화. 단 피처_정의서 §3·ML_설계 개정 + 02장 재작업 (P18 Q4) | 이득이 크므로 **채택 검토 권고** — 단 8/3 마감 대비 작업량을 새 세션 초두에 산정 후 결정 |
| 2 | jupytext 도입 | 사이클 diff 가독성 개선 옵션 (P18 Q3) | 보류 유지 — diff가 고통스러워지면 재론 |
| 3 | `eda-workflow` 스킬화 | 확정 흐름을 Dacon 허브 skills/로 패키징 (피드백 ③ "스킬처럼") | 노트북 개정 완료 후 별도 작업 |

## 7. 새 세션 착수 순서 (권장)

1. 이 문서 + P18 + ITERATION-LOG 읽기 → 미결 1(martj42) 결정
2. `00_collect_explore.ipynb` 작성 (§3) — 마스킹 셀 이동 포함
3. 01·02장 보강 (§2 표) — 개정 이력 셀·pandera 게이트, ITERATION-LOG에 c2 기록
4. 검증: 00→02 순차 실행 + 리키지 assert + 실명 전수 검색 0건
5. 03장(LightGBM)부터는 개정된 규약으로 신규 작성
