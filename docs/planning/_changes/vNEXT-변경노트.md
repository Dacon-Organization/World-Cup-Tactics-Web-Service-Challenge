# vNEXT 변경노트 (버전업 전 누적)

> 다음 버전업(마일스톤 단위 일괄) 때 이 노트를 재료로 "변경사항" 문서를 생성합니다.
> 규칙: 전역 §7.1 · [마스터인덱스](../마스터인덱스.md) 문서 운영 규칙 1항

## 누적 중인 변경 (features vNEXT / ui vNEXT)

| 날짜 | 대상 | 변경 | 사유 |
|---|---|---|---|
| 07-22 | F01 | `[NEEDS CLARIFICATION: 토큰 표기]` → 확정값(가공명 성 1음절+포지션 색상 링)으로 치환 | [가공명_체계 §5.1](../data/version1.0/가공명_체계_v1_0.md) 게이트 해소 |
| 07-22 | F08·ADR-004 | `[NEEDS CLARIFICATION: 기본 상대]` → 체코(cze) 확정으로 치환 | [가공명_체계 §5.2](../data/version1.0/가공명_체계_v1_0.md) 게이트 해소 |
| 07-22 | F01~F16 전체 | 기능별 상태/시퀀스 다이어그램 보강 + 해설 상세화 | 사용자 피드백: "쉽고 상세하게"(포트폴리오·회고 자료화). 전 기능 일괄이므로 v1.1 마일스톤로 |
| 07-22 | F10·데이터_설계 | 이벤트 스키마 `[설계 결정]`을 데이터_설계 §2.3 확정 스키마 참조로 치환 | 데이터 설계 v1.0 확정 |
| 07-23 | F04·ADR-002 | "모델 입력 피처로 매핑 — ML 설계 단계 구체화" 참조를 ML 설계 v1.0(피처_정의서 §4·ADR-008) 링크로 치환. ADR-002 Consequences 잔여 과제 해소 각주 추가 | ML 설계 v1.0 확정 — 슬라이더는 학습 피처가 아닌 조정 계층으로 확정 |
| 07-23 | 노트북_서사 | 00장(수집·탐색) 신설로 10장 구조화: §2 표·§3 카드에 00 추가, §4 마스킹 경계 "01장 서두"→"00장 로드 직후", §5 실행 순서 "00→09" | 사용자 피드백 ①(수집 서사·탐색 상세화) — 상세: [노트북-개정안-2026-07-23](노트북-개정안-2026-07-23.md) |
| 07-23 | 노트북_서사 | §6에 NB-R7(사이클 개정 시 이력 셀+ITERATION-LOG 동시 기록)·NB-R8(pandera 게이트 실패 시 하류 중단) 신설, 반복 사이클 규약 절 추가 | 사용자 피드백 ②(반복 사이클 버전 관리) — [notebooks/ITERATION-LOG.md](../../../notebooks/ITERATION-LOG.md) 신설됨 |
| 07-23 | ML_설계·피처_정의서 | 도구 채택 반영: fg-data-profiling(00장 프로파일)·pandera(전 장 게이트, `src/analysis/schemas.py`)·missingno(결측 시각화)·jupytext(조건부) + data/raw·interim·processed 계층 | P18 리서치 회수 — 사용자 피드백 ③(고스타 도구 커스터마이징) |
| 07-23 | 피처_정의서 §3·ML_설계 | **Elo 입력을 월드컵 한정 → 전체 남자 A매치(1872~)로 확장** — 2차 출처 martj42/international_results(CC0) 채택, FT-R7(공백 감쇠)은 c2 실측으로 재평가(재추정 또는 제거) | 사용자 확정 2026-07-23 ("이득이 크므로 채택") — 근거 P18 Q4, 통합 설계 [개정안 §6.5](노트북-개정안-2026-07-23.md) |
| 07-23 | 피처_정의서 §3 (FT-R7) | **감쇠 φ 제거 확정** — c2 실측: 대회 첫 경기 시점 공백 중앙값 1,456일→12일, Elo↔승률 ρ 0.221→0.308. DB-04' 신설(HA=100 기대 스코어 한정·S는 최종 스코어). 신생팀 초기값 1500·승계 미연결(체코슬로바키아↔체코)은 03장 재검 | c2-2 완료 — [ITERATION-LOG c2 완료 기록](../../notebooks/ITERATION-LOG.md) |
| 07-23 | 노트북_서사 §5 | 실행 순서 "01→09" → "00→09" 실현 · 03장 입력 계약 features_v0 → **features_v1** · requirements.txt 확정(버전 고정) | c2 전체 완료 |
| 07-23 | **원고 10절(ML아키텍처)** | 조판 블록 c2 동기화 — Elo 입력 A매치 확장 서술(49,520경기·공백 1,456→12일·ρ 0.221→0.308) 반영, 파이프라인 다이어그램에 Elo 입력(전체 A매치 CC0)/학습 피처(월드컵 964) 분리, 한계 고지 "본선 공백 팀 증거 약함"→"신생팀 초기값 1500·승계 미연결(체코슬로바키아↔체코)", Elo 궤적 그림(02_elo_trajectories.png c2 갱신본) 조판 태그 신설 | c2-2 실측 반영 — [ITERATION-LOG](../../../notebooks/ITERATION-LOG.md) |
| 07-23 | **원고 9절(데이터활용)** | 조판 블록 c2 동기화 — 2차 출처 `martj42/international_results`(CC0 1.0, Elo 산출 입력·실명 없음) 추가: 학습층 다이어그램 A2 노드·서술·라이선스 고지, 1차 출처 표기를 `jfjelstul/worldcup`로 통일 | [DATA-LICENSE.md](../../../DATA-LICENSE.md) 2차 출처 절과 정합 |

| 07-27 | **데이터_설계 §2.3 (MatchEvent)** | 교체 이벤트에 `direction: 'in' \| 'out'` 필드 추가 필요 — 현재 스키마는 "in/out 2행"만 정하고 방향 필드가 없어 **배열 순서(OUT→IN)에 의미를 싣고 있다** | B2 구현 중 발견. 기획서 PDF 동결이라 v1.0 스키마는 그대로 두고 `src/data/README.md`에 순서 규약을 명시. F10(타임라인) 구현 전 v1.1에서 해소 |
| 07-27 | **데이터_설계 §1 (ERD 해설)** | 엔티티 표의 예시에서 실명 1건 → 추상 표기("남아공전 63' 득점자")로 치환 | G1 게이트 실측에서 적발. 가공명_체계 §1이 정한 적용 표면("공개 문서 전부") 위반이었음 |
| 07-27 | **제출-게이트 §1 G1·G3** | G1에 `docs/research/**` 완전일치 제외 규칙(적중 건수 상시 출력 조건부) + 일반어 충돌 실명 제외 규칙 추가. G3에 `브라우저전달` 기반 예외 체계·폰트 고지 파일 검사 추가 | B2에서 게이트를 실제로 구현하며 확정. 문서와 스크립트가 어긋나면 어느 쪽도 못 믿게 되므로 동시 갱신 |
| 07-27 | **DESIGN.md §2 (타이포)** | 서체 패밀리명 `Pretendard` → **`RF Sans`**(Pretendard 서브셋 파생본). 아웃라인 동일 | OFL 1.1 §3 · OFL FAQ 2.6 — 웹폰트 서브셋은 수정본이라 예약 폰트 이름(RFN) 사용 불가. 근거: [public/fonts/NOTICE.md](../../../public/fonts/NOTICE.md) |
| 07-27 | **DESIGN.md §3.4** | f442·f343·f352 좌표 신설 확정 (좌우 대칭 · 라인 간격 0.20~0.26) | B2 `src/data/formations.json` — 규칙 준수는 `tests/data.test.ts`가 고정 |
| 07-27 | **피처_정의서 §4.1 (전달 경로)** | λ→확률 전달 경로에 **무승부 몫 채널 신설**: `p_draw ← p_cls.draw + (p_λ(λ_adj).draw − p_λ(λ⁰).draw)` 후 승/패 비례 재정규화. 기존 Δ_eff 채널은 승/패 **비율** 전담으로 역할 분리 | 현행 §4.1은 전달 경로가 Δ_eff 하나뿐이라 **템포가 확률을 전혀 못 움직인다**(정의상 λ 비율 불변). F04 수용 기준 "4종 각각 확률 변화"와 §4.2 자신의 서술("열린 경기는 강팀 상대 시 불리")이 둘 다 깨진다. 근거·검증: [ITERATION-LOG c3](../../../notebooks/ITERATION-LOG.md) |
| 07-27 | **피처_정의서 §4.5 (계수 초기값)** | δ·κ·ρ·c 확정값 기록 예약분 채움 — `c`=1.0 · 라인 δ 0.3752/κ 0.1251/δ' 0.1876 · 압박 δ 0.1592/δ'' 0.0764/κ 0.0637 · 폭 δ 0.3306/κ 0.1587 · **템포 δ 0.2624 = ln(1.30)** · ρ_pen 0.06. 배치 파생 정규화 폭 `posLineSpan` 0.20 · `posWidthSpan` 0.10 신설 | 03장 이분법 캘리브레이션. **템포만 이분법에서 제외**한 이유: 확률 경로가 무승부 몫 하나뿐이라 이득을 키우면 z=±1 이전에 ±30% 클램프에 닿아 슬라이더가 계단 함수가 된다 → "z=±1에서 정확히 상한"으로 고정 |
| 07-27 | **피처_정의서 §2·ML_설계 §4.1** | 학습 시 **미러 증강**(자기/상대 교환 행 추가) 규약 신설 — 배포 모델은 `p_승(x) = p_패(mirror x)`를 구조로 만족 | 대회 데이터의 `home` 지정을 상속하면 Elo 동률에서 승 0.50 vs 패 0.22(**격차 28.5%p**)가 한국에 무료로 붙는다. 중립 개최지 서비스와 어긋나며, 실측상 valid RPS도 대칭화가 더 좋다(0.2096→0.1975) |
| 07-27 | **ML_설계 §4.2 (스코어 모델)** | Dixon-Coles τ에 **대각 보정 항 γ** 추가 `τ ← τ·exp(γ·1{x=y})`, γ는 우도가 아니라 **정합 목적함수(train TV)** 로 적합 | ML-R8 발동(valid TV 0.0594 > 0.05). 괴리의 대부분이 무승부 성분 — 독립 포아송이 무승부를 과소 예측하는데 DC τ는 저점수 4칸만 손댄다. γ=+0.1558로 TV 0.0440(통과). 우도 적합은 γ<0을 내놓아 TV가 악화되므로 목적함수 분리가 필수 |
| 07-27 | **ML_설계 §5.1 (ONNX 계약)** | 배포 입력 시그니처를 **10원소 고정 벡터**로 동결(`elo_diff`+자기 4+상대 4+`stage_ko`), 출력 `probabilities [N,3] = [승,무,패]`, `zipmap=False`. `stage_ko`는 팀별 중복 제거 | 계획서 문구("Elo 차이 covariate 기준선")대로 1원소로 얼면 06장 GBDT가 계약을 깨서 프론트엔드를 재작업해야 한다 — 계약 동결의 목적과 정면 충돌. Elo 단독 로지스틱은 비교 기준선으로만 측정(valid RPS 0.2241) |
| 07-27 | **평가_설계 §2.3 (EV-R4)** | 캘리브레이션 임계 판정에 **빈별 이항 표본오차 밴드 병기** 규칙 추가. valid 64경기×3범주=192쌍은 10빈으로 나누면 빈당 십수 개라 무편향에서도 0.10을 넘는다 | ⚠️ **divergence 적재**: 현재 배포는 EV-R4 임계 미충족(최대 편차 0.2166 > 0.10). 처방(온도 스케일링)을 진단으로 돌린 결과 T=1.0537(사실상 항등)이고 적용 시 편차가 0.2390으로 **악화** → 잡음에 모델을 맞추지 않고 미적용. 06장 재검 항목 |
| 07-27 | **평가_설계 §1 (방어선)** | ⚠️ **divergence 적재**: 기준선의 valid 정확도 0.4844 — 밴드 0.50~0.55 **미달** (RPS 0.1975는 충족) | 06장 GBDT 재측정 대상. 미달이 지속되면 ML_설계 §7.1 3단(측정값 그대로 공개) |
| 07-27 | **DESIGN.md §1.4 (형태 분리)** | **"배열 = 모난 사각" 규칙 철회.** 원본 f12 ①번 패널은 **원 + 1색 채움 + 중립 미채움**("100번 중 62번")인데 구현이 모난 사각 + 승·무·패 3색이었다. 원본 부호화로 복귀하고, 결과 선택 탭(승/무/패)으로 정보량을 보전 | §1.4의 근거("토큰이 원이니 배열은 사각")가 **원본 배열도 원이라는 사실을 놓친 것**. §0의 "문서와 원본이 어긋나면 원본 우선" 규칙 적용. 부수 효과로 **한 화면에 결과색이 하나만** 남아 포지션 4색과의 충돌 표면이 소멸 — 팔레트 변경 없이 §1.4 "미해결" 항목이 해소됨 |
| 07-27 | **DESIGN.md §7 (라이트 모드)** | `[설계 결정] 라이트 모드 = 이득 없음, 다크 단일`을 **뒤집음**. 라이트 모드 도입, **기본값은 다크 유지**(f00 표지·f13 공유 카드와 첫 화면 일치). `:root[data-theme='light']` 오버라이드 + `localStorage` 명시 선택. `prefers-color-scheme`은 따르지 않음 | 결정 당시 근거는 "7일 내 QA 비용". 실제 비용은 토큰이 전부 시맨틱이라 오버라이드 블록 1개 + 컴포넌트 재작성 0건. **라이트 팔레트도 역추출로 성립** — `make_figures.py:38~48`의 `INK/SOFT/GRID`와 f10 와이어프레임 보드(`facecolor="#F7FAF8"` + GREEN 라인 alpha 0.8)가 그대로 라이트 피치가 됨 |
| 07-27 | **DESIGN.md §1.1 (라이트 텍스트 4계단)** | 라이트 램프 신설 — `text #1A1D24 · text-2 #4B5663 · text-3 #57636F · text-4 #616E7C` (bg `#EEF1F5` / surface `#FFFFFF`). 파생색 `--color-accent`(다크 `brand-lit` ↔ 라이트 `brand`) · `--color-pitch(-line/-accent)` · `--color-token-bg` · `--color-neutral` 신설 | 동결 `GREY #98A2B3`를 그대로 쓰면 흰 배경 대비 **2.58:1**로 본문 미달(원 그림에서 주석선용이지 본문색이 아님). 4단 전부 4.5:1을 넘도록 계산해서 유도 — bg 기준 6.60/5.42/4.60. **실측: 라이트 대비 미달 0건** |
| 07-27 | **DESIGN.md §6 (명도 대비)** | ⚠️ **divergence 적재**: §6이 `--text-4 (#55636F on #10161F) ≈ 3.9:1`로 적었으나 **실측 2.94:1**. `--text-3`(#6B7A8C)도 surface 위 **3.74:1**로 본문 4.5 미달 → §8 체크리스트의 처방("미달 시 text-3로 승격")이 **작동하지 않음**(승격 대상도 미달) | 다크 기본에서 **미달 17건**(전부 이 두 색). 두 값 모두 f13에 실린 동결 팔레트라 이번 블록에서는 손대지 않고 적재만 함. 색상 유지 + 밝기만 올린 준수 램프는 산출해 둠(`#A7BED5`/`#91A5B9`/`#8092A4` — 5.12~8.56). **B8 결정 항목** |
| 07-27 | **DESIGN.md §1.3 (결과 3색 라벨)** | 확률 막대 세그먼트 **글자색**을 결과별로 분리 — 승·패는 흰색, **무(앰버 #C98A21)는 잉크 #1A1D24**. 배경(동결 3색)은 불변 | 앰버 위 흰 글자 실측 **2.94:1**(18px/700 → 4.5 요구). f13이 쓴 조합이지만 인쇄물과 달리 화면 기준에 걸림. 잉크 글자는 **5.73:1**. 그림과 대조되는 부분(막대 색·폭·라벨 문구)은 그대로 |
| 07-27 | **DESIGN.md §3.2 (세로 예산) · 화면흐름 S1** | 예측 패널을 **요약 도크**(하단 고정)와 **상세**(본문 흐름)로 분리. 조작 순서를 `헤더 → 피치 → 전술(포메이션+슬라이더) → 예측 상세 → [고정] 요약`으로 재배치. `<details>` 아코디언 2개 중 포메이션은 세그먼트 컨트롤로 전환 | 실측: 예측 패널 **749px**(예산 132px의 5.7배) · 첫 슬라이더 top **1505px** → **슬라이더를 조작하는 동안 확률이 화면에 없었음**(U4 2단계 불성립). 화면흐름 v1.0과 f10이 처음부터 "예측 패널(하단 고정)"으로 정해 둔 구조라 신설이 아니라 미구현분. 재측정: 헤더 53·피치 374·도크 89로 첫 화면 예산 충족, 도크는 전 스크롤 구간에서 고정 |
| 07-27 | **DESIGN.md §3.2 (도크 높이 상한)** | 도크 높이 상한 **110px** 규칙 신설 — 넣을 수 있는 것은 대표 숫자와 3분할 막대뿐. 출처 문구·신뢰구간·배열은 상세로 | `position: sticky`는 **부모 상자 위로 올라가지 못함**. 도크의 부모(우측 열)가 375×667에서 y=533부터라 첫 화면 가용 세로가 `667-557=110px`. 첫 구성(160px)은 실측에서 50px 잘렸음 — 실측으로 확정한 값 |
| 07-27 | **DESIGN.md §2 (타이포) · §4** | 40px/700 확률 대표 숫자를 **실제로 도입**(그전까지 화면 최대 글자가 22px 제목이었음). 16px 기본값 누출 제거 · gap을 4px 스케일 값으로 통일(6px·20px 제거) · f00 2색 워드마크(`RE:` + `FORMATION`) 이식 · 슬라이더 채움을 **크롬·사파리에도** 그림(`::-moz-range-progress`만 있어 지금까지 파이어폭스에서만 보였음) | §2 표가 정의해 둔 값이 화면에 없었고, 정의에 없는 16px이 9곳 새어 있었음. 재측정: gap 스케일 밖 0건 · 카드 규격 3회 이상 반복 0건 · 렌더되는 글자 전부 §2 표 안 |
| 07-27 | **제출-게이트 G1** | ⚠️ **실행 불가 상태**: 대조 목록 `data/gate/real-names.txt`가 이 머신에 없음(메인 체크아웃에도 `data/gate/` 부재). `data/`는 gitignore이고 worktree마다 독립이라 **이전 worktree와 함께 소실된 것으로 추정** | 이번 변경과 무관(변경분에 인명 0건 확인). 다만 **8/2 최종 커밋 전 반드시 복구**해야 G1이 의미를 갖는다. 목록은 한글 실명 1줄 1건 형식(`names.ts`가 초성·로마자 변환해 대조) |
| 07-27 | **B3 산출물 정합 (조정 계수 정밀도)** | 03장이 `K_ADJ`를 **전체 정밀도**로 들고 픽스처·민감도를 돌면서 `score-params.json`에는 `round(v, 8)`로 실었다 → 배포 상수로는 픽스처를 1e-9로 재현할 수 없었다. **수정**: ① 노트북에서 `K_ADJ`를 정의 시점에 8자리로 굳혀 이후 전 경로가 배포값을 쓰게 함 ② `scripts/verify/adjust-fixture.py`로 `adjust-cases.json` 재생성(배포 상수 기준) | B4 실측: JS 대조 최대 오차 Δ_eff **1.88e-6** · λ 상대 **1.89e-8** (허용 1e-9). 원인 확정 증거 — 템포는 λ 비율을 안 바꾸는데 양쪽 λ가 **정확히 같은 비율 −4.467491e-9**로 어긋났고, 이는 `deltaTempo = ln(1.3) = 0.2623642644674911`을 `0.26236426`으로 실은 차이와 **소수점까지 일치**. 재생성 후 최대 오차 **4.98e-11**(픽스처 10자리 저장 반올림 한계) |
| 07-27 | **구현규약 §3 (predictionSlice)** | `context: TacticContext \| null` 필드 추가 — 조정 계층 산출물을 B4가 쓰고 B5 Worker가 읽는 인계 지점 | §3 슬라이스 정의에 조정 계층 산출물의 자리가 없었다. `p`에 직접 쓰면 F05-R2("추론 중 직전 값 유지")가 깨지므로 별도 필드가 필요 |
| 07-27 | **구현규약 §5 (`AdjustInput`)** | 시그니처 확장: `roles` → `slotRoles` + `playerRoles` + `presetSlots` | 문서의 `roles` 하나로는 파이썬 참조 구현을 재현할 수 없다 — 적합도 페널티(§4.4)는 **슬롯 역할과 선수 포지션의 차이**를 쓰고, 배치 파생 지표(§4.3)는 **프리셋 기준 배치**를 원점으로 쓴다. 세 입력이 전부 필요 |
| 07-27 | **F03 §4 (스냅 규칙)** | "클릭-클릭·키보드도 드롭존 스냅 규칙 동일 적용" → **키보드 경로는 자기 홈 슬롯을 스냅 후보에서 제외**한다는 단서 추가 | ⚠️ B4 브라우저 실측에서 발견한 **실제 결함**: 방향키 1회는 2%인데 스냅 반경은 8%(피치 높이 단위)라, 홈 슬롯이 후보로 남으면 **3번까지 누른 이동이 전부 제자리로 끌려와 아무 일도 일어나지 않는다**(2칸 이동 → Enter → 원위치, 6칸부터 커밋). F03 GWT "Enter Then 이동이 완료되고"가 짧은 이동에서 성립하지 않아 **WCAG 2.5.7 대안 경로가 형식적으로만 존재**하게 된다. 드래그는 손가락이 크게 움직여 홈 슬롯 자석이 이득이므로 그대로 둔다 |
| 07-27 | **F01 §5 (F01-R4 실측)** | `[추정 설계목표]` 해소 — 280×653에서 가로 스크롤 0 · 토큰 44px 유지 · 11명 전원 피치 안 · 겹치는 쌍 1건 | B4 브라우저 실측. 겹침이 조작을 막지 않아 토큰 하한 44px를 유지한다 |
| 07-27 | **데이터_설계 §2.2 (`PositionSlot`) · F01 토큰 라벨** | 슬롯에 **세부 포지션 약어 `code`** 신설(`GK/LB/LCB/CB/RCB/RB/LWB/RWB/DM/LCM/RCM/LM/RM/LW/RW/LST/ST/RST`). 토큰 라벨을 **가공명 성 1음절 → 포지션 약어**로 교체하고 `tokenLabel()` 제거. Zod에 한 포메이션 내 약어 유일성 `refine` 추가 | 성 1음절은 26명 중 성이 겹치면 **같은 글자가 둘** 뜬다(사용자 피드백). 등번호는 ADR-005 결정 3이 금지(`등번호+포지션` 조합 = 연상 리스크)하므로, **선수가 아니라 슬롯에 붙는** 포지션 약어로 우회. 좌우 접두사(FM의 `DCL/DCR` 방식)로 4개 포메이션 **전부 11개 고유**. 표기 체계는 [Wikipedia 축구 포지션](https://en.wikipedia.org/wiki/Association_football_positions)의 공용 약어 — EA(`CAM/CDM`)·Konami(`-MF`) 등 게임사 고유 표기는 쓰지 않음 |
| 07-27 | **DESIGN.md §3.2 · 화면흐름 S1 (데스크톱 골격)** | 데스크톱을 **"피치 좌 2/3 · 패널 우 1/3" → "가로 피치 위 · 전술 바 아래(좌 전술 · 우 예측)"** 로 전환. 모바일은 세로 1열 유지. 슬라이더는 데스크톱에서 2×2. `main` 폭 상한 1024 → 1280px | 실측(1280×720): 오른쪽 열이 **320px 폭에 1360px**(뷰포트 높이의 1.9배)을 쌓고 왼쪽 640px에는 **269px 피치**만 있어 좌우 높이비 **1:3.1**. 세로 피치의 폭이 가용 **높이**에 묶여(`max-width: calc(56dvh*2/3)`) 가로를 더 줘도 커지지 않는 구조였다. 재측정: 피치 **518×346**(폭 **1.93배**) · 슬라이더 4개 전부 y≤710으로 첫 화면 · 확률 요약 y=482로 슬라이더와 동시 노출 |
| 07-27 | **F01 impl (방향 전환 구현 방식)** | 정규화 좌표(`x` 좌→우 · `y` 자기진영→상대진영)는 **불변**. 화면 매핑만 CSS 미디어쿼리(`min-width: 1024px`)로 전환 — 토큰은 `--nx/--ny` 사용자 정의 속성 + `calc()`, SVG는 상자를 눕혀 `rotate(90deg)`(viewBox는 세로형 그대로). JS는 **포인터 역변환·방향키 축·카드 개폐 방향** 세 곳에서만 방향을 안다 | 방향을 React 상태로 들면 서버가 뷰포트를 모르므로 데스크톱 첫 페인트가 세로로 나왔다가 튀고, JS가 죽으면 영영 세로다(F08-R5 퇴행). CSS로 두면 SSR HTML이 데스크톱에서도 처음부터 가로다. 부수 효과로 `pitchDistance`·`nearestSlot`·`resolveDrop`·조정 계층·예측 엔진이 **한 줄도 바뀌지 않음** |
| 07-27 | **F03-R6 (방향키 축)** | `applyKeyboardStep`에 `orientation` 인자 추가 — 가로에서 `→`=전진(`y+`), `↑`=팀의 왼쪽(`x-`) | 방향키는 **화면 기준**이어야 한다. 가로에서 `↑`가 그대로 `y+`(전진)이면 토큰이 오른쪽으로 가면서 위 화살표를 누르게 돼 조작이 즉시 무너진다 |
| 07-27 | **F01 (선수 정보 카드) · ADR-005** | 토큰 선택·호버 시 뜨는 **비모달 팝오버** 신설 — 가공명 + 포지션 한글명 + 프로파일 4종(공격·수비·체력·속도). 호버는 420ms 지연, 터치 기기 제외(`@media (hover: hover)`) | 토큰 안이 약어로 차면서 **가공명이 화면 어디에도 없어졌다**. 모달로 만들면 안 되는 이유: 토큰 탭은 F03 클릭-클릭 이동의 "선수 선택"이고 다음 탭이 "목적지"라, 포커스 트랩·배경 차단이 들어가면 이동 경로가 죽는다. `pointer-events: none`으로 카드 위 탭이 그대로 피치에 닿게 함. 지금까지 화면에 없던 `profile` 4종이 처음으로 노출됨 |

| 07-28 | **F12 §3·§8 · DESIGN.md §4 (`WinProbChart`)** | "**538식** 2라인(실제 실선 · 시나리오 점선+다른 색)" → "**결과 선택 + 2라인**". 2라인 구성은 유지하되 계열의 의미가 "승률"이 아니라 **"선택된 결과(승/무/패 중 하나)의 확률"** 이고, 계열 전환은 D-1이 확률 배열에 세운 결과 탭 규약을 재사용 | P20 회수 — **538의 시간축 승률 차트는 무승부가 없는 종목(NFL·NBA)의 것**이다. 축구는 Opta LWP("win, draw or loss" 3값 동시 표기)·arXiv 1906.05029 Figure 1("minute-by-minute win/tie/loss" 3곡선)·inpredictable이 일치해 **3결과 체계**. 승 1선만 그리면 무승부가 사라지고, 3계열×2시나리오=6선은 375px에서 안 읽힌다. [findings/P20](../../research/findings/P20-리플레이UI패턴.md) |
| 07-28 | **F10 §3 · 데이터_설계 §2.3** | 모멘텀 차트(Sofascore·Opta 방식) **채택 불가 사유를 명문화** — 분 단위 이벤트 데이터를 전제하는데 1차 데이터셋에 없고 StatsBomb 재업로드는 P10이 채택 금지 | 심사자 예상 질문(T5) 대비. 없는 데이터를 그럴듯하게 그리는 것이 더 큰 감점이다 |
| 07-28 | **데이터_설계 §2.3 (`Match`)** | `city: string` 필드 신설 — `과달라하라`(cze·mex) / `몬테레이`(rsa) | 코드에 `matchId → 도시` 표를 두면 F09-R2("카드는 `matches.json`에서만 렌더")를 어기고 사실이 두 곳에 존재한다. **전 경기 멕시코 개최**(P11 정정)를 화면이 스스로 드러내는 필드이기도 하다 |
| 07-28 | **화면흐름 S2a (진입 카피 위치)** | "카드 **하단**에 진입 카피" → **`h1` 자리**. 문구("이 경기, 당신이라면?")는 불변 | 카피를 하단에 두면 이 화면에 `h1`이 없어져 스크린리더 문서 개요가 비고 공유 미리보기 제목도 사무적 문자열이 된다. 카피 자체가 화면의 질문이므로 제목으로 정확 |
| 07-28 | **화면흐름 S1 (리플레이 진입점)** | "헤더의 **모드 탭**" → **헤더 우측 텍스트 링크**(`리플레이`) | 모드 탭은 헤더를 2줄(+40px)로 만든다. D-1이 헤더를 106→52px로 깎은 것이 "조작 중 확률이 보인다"(U4 2단계)의 근거였으므로, 그 예산을 되돌려 쓰지 않는다. 실측: 링크 대비 다크 8.15:1 · 라이트 7.65:1 |
| 07-28 | **F09-R3 (구현 형태)** | "S2a로 안내 리다이렉트"의 구현을 **`redirect('/replay?unknown=…')` + 목록 상단 `role="status"` 안내**로 확정. `notFound()` 미사용 | 리다이렉트만 하면 사용자는 자기가 왜 목록에 있는지 모른다. 부작용으로 `/replay`가 정적→동적 렌더가 되지만(빌드 출력 `ƒ`), 페이지가 카드 3장이라 비용이 무시할 수준. 실측: `/replay/xyz` → 안내 노출·카드 3장·404 0건 |
| 07-28 | **DESIGN.md §1.1 (다크 텍스트 램프)** | 다크 `--color-text-3` **#6B7A8C → #7C8B9D**. 카드(#1A2028) 위 3.74 → **4.71:1**, 페이지(#10161F) 위 4.14 → **5.22:1** | 07-27 39행 divergence("승격 처방이 작동하지 않음")의 **해소**. 그때는 램프를 f13 동결 팔레트로 보고 적재만 했으나, `globals.css`가 동결로 규정한 것은 **결과 3색·포지션 4색·브랜드 2색**이고 라이트 램프는 D-1이 이미 재계산해 넣었다 — 다크만 미검증으로 남아 있던 것. **사용자 결정(2026-07-28)**. D-1이 산출해 둔 3색 램프(#A7BED5/#91A5B9/#8092A4) 대신 text-3 한 값만 옮긴 이유: #91A5B9는 text-2(#8E9CAD)보다 밝아 **램프 순서가 뒤집히므로** 3색을 함께 올려야 하고, 그러면 변경 범위가 이 세션 밖으로 나간다. ⚠️ **잔여**: `--color-text-4` 고지문 다크 **2.94:1** 미달 유지(별도 과제) |
| 07-28 | **DESIGN.md §4 (컴포넌트 인벤토리)** | `MatchCard` 규격 확정 — 국기 + 라운드 + 스코어(40px/700) + **무채색 결과 배지** + 득점 분 요약 + 개최지·일자. **결과 3색 미사용**. `Flag` 컴포넌트 신설(32/44px · 코드 폴백 2층 구조) | 한국 3경기는 1승 2패라 스코어에 결과색을 쓰면 **한 화면에 결과색이 세 개**가 뜬다 — D-1이 회복한 "한 화면에 결과색 하나"가 화면마다 예외를 갖기 시작하면 근거가 흐려진다. 승/패는 글자가 전달(색맹 대응 동시 충족) |
| 07-28 | **DESIGN.md §6 (비텍스트 대비)** | 국기 테두리 알파 **22% → 55%** (라이트 1.59 → **3.81:1** · 다크 2.05 → **5.85:1**) | 22%는 선수 토큰 링에서 가져온 값인데, 실측하니 "경계를 보장한다"는 선언의 목적을 달성하지 못했다. ⚠️ **같은 문제가 `.player-token` 링에 그대로 있음**(라이트 1.58 · 다크 2.04) — S1 범위라 별도 과제로 분리 |
| 07-28 | **제출-게이트 §1 G3** | 에셋 고지 검사에 `public/flags/NOTICE.md` 추가 | 국기는 npm 의존이 아니라 **파일 복사**라 `node_modules` 스캔에 잡히지 않는다. 고지가 사라져도 아무도 모르는 상태를 막는다. 실측 근거(멕시코 국기 84,753 B = 4종 raw의 94.6% · npm 패키지 4.13 MB)는 [findings/P20](../../research/findings/P20-리플레이UI패턴.md) |

## 기록 형식

- 한 줄 = 한 변경. `대상`은 문서 ID(F05, S2b, U1 …) 단위로.
- 스펙 변경뿐 아니라 **구현 중 발견된 예외·버그의 명세 반영**도 여기 누적 (선행 프로젝트 피드백: 발견 사항을 버전 관리로 기록·다듬는 구조)
